1) Install ImageMagick
2) Make sure the command "convert" from ImageMagick can be executed
3) Put both scripts (*.cmd) into the folder with BMPs
4) run "startconvert.cmd"
5) It will create PNG out of all BMP with correct transparency